apiVersion: v2
name: rag-platform
description: Plataforma RAG — perguntas e respostas sobre documentos privados
type: application
version: 0.1.0
appVersion: "1.0.0"

maintainers:
  - name: rag-platform

dependencies:
  - name: redis
    version: ">=20.0.0"
    repository: https://charts.bitnami.com/bitnami
    condition: redis.enabled

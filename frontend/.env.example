# Copiado de .env.example para .env e preenchido localmente
# NUNCA commite o .env com valores reais

# URL base da API (em dev, o proxy Vite redireciona /api para o backend)
# Em prod, defina a URL completa do backend
VITE_API_BASE_URL=/api
